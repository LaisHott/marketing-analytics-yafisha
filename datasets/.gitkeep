## 📁 Sobre os Dados

O projeto utiliza três arquivos principais de dados:

### 🧭 `visits_log.csv`  
Contém registros dos acessos dos usuários ao site ou app da Y.Afisha.

**Colunas esperadas:**
- `user_id`: identificador único do usuário  
- `visit_date`: data da visita  
- `region`: localização do usuário  
- `device`: tipo de dispositivo usado (desktop, mobile, etc.)

---

### 🛒 `orders_log.csv`  
Contém os pedidos feitos pelos usuários — eventos de conversão (compra).

**Colunas esperadas:**
- `user_id`: identificador único do usuário  
- `order_date`: data da compra  
- `revenue`: valor monetário da compra  
- `device`: dispositivo usado na compra  
- `region`: localização do usuário

---

### 📊 `costs.csv`  
Contém os investimentos em marketing por canal e por dia.

**Colunas esperadas:**
- `source`: canal de marketing (ex: Google, Instagram, Facebook)  
- `cost_date`: data da despesa  
- `costs`: valor gasto em marketing no canal naquele dia
